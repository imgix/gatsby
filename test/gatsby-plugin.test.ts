// / <reference types="../types/gatsby" />
/// <reference types="jest" />

test('water is wet', () => {
  expect(1);
});

// describe('onCreateNode (gatsby-plugin)', () => {
//   describe('basic case - valid url', () => {
//     it(`should return a valid src in the created field`, async () => {
//       const mockCreateNodeFieldFunction = jest.fn();
//     });
//   });

//   describe('url is invalid');

//   describe('on init', () => {
//     it('should throw an error if an invalid field configuration is passed');
//     it(`should throw if sourceType isn't 'webProxy'`);
//     it(`should throw if secureURLToken is not set`);
//   });
// });
